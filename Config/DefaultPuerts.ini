[/Script/Puerts.PuertsSetting]
; 关闭 Puerts 自带的自动模式：TS 入口由本框架的 UPuertsHost（AiGameScriptHost 插件）
; 在 GameInstanceSubsystem.Initialize 中显式创建 FJsEnv 并 Start("Main") 接管。
; 若误设为 True，Puerts 会额外自建一个 JsEnv，与本宿主冲突（双虚拟机）。
AutoModeEnable=False
